## Reference: MemoryintheAgeofAIAgents: ASurvey

Source document: `docs/2512_13564.pdf` (107 pages)

### How to use the document map

A semantic map of this document is available at three levels of detail:

1. **This file** — hand-curated invariants and guidance (you are here)
2. **`docs/2512_13564_MAP.md`** — machine-generated section map with typed
   claims, symbol definitions, and dependencies. Read this for any question
   about what the document says. Replaces reading the raw PDF in most cases.
3. **`docs/2512_13564.pdf`** — the original. Read only when you need exact
   wording, figures, or proofs.

### Querying the indexes

Two JSON indexes support programmatic lookup:

**Symbol lookup** (`docs/2512_13564.symbols.json` — 126 symbols):
```bash
# Find where a symbol is defined
python3 -c "import json; [print(f'Defined in §{s[\"defined_in\"]} p.{s[\"defined_at_page\"]}:  {s[\"meaning\"]}') for s in json.load(open('docs/2512_13564.symbols.json')) if 'QUERY' in s['symbol']]"
```

**Claim queries** (`docs/2512_13564.anchors.json` — 283 claims):
```bash
# List all caveats
python3 -c "import json; [print(f'p.{c[\"page\"]} {c[\"text\"]}') for c in json.load(open('docs/2512_13564.anchors.json')) if c['type'] == 'caveat']"

# All claims in a specific section
python3 -c "import json; [print(f'[{c[\"type\"]}] {c[\"text\"]}') for c in json.load(open('docs/2512_13564.anchors.json')) if c['section'] == 'SECTION_ID']"
```

<!-- Generated by mapping-documents v0.1.2 -->
<!-- Paste into CLAUDE.md, AGENTS.md, or Claude.ai project knowledge -->